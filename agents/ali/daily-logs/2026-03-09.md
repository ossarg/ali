# Daily Log — 2026-03-09 (Lunes)

## Resumen del día

- **Sin sesión humana directa con Ali** — actividad técnica de Ali (Woz) y Nacho en el repo.
- **9 commits** en `main` entre las 21:21 y las 22:58.
- **Tema del día: deploy a producción + auth real.** Primera puesta en marcha documentada del stack completo (backend + frontend + docker) en entorno real con Nacho.
- Se creó `docs/design-system.md` (345 líneas) y se hizo el primer backup de DB registrado en el repo.

---

## Evento principal 1: Deploy a producción — fixes de infra

### Cambios
- **VITE_API_URL vacío** en `axios.client.ts` → usa proxy Vite (requests relativas al origen). Fix crítico para deploy: evita hardcoding de URL del backend.
- **fix(docker): restart:unless-stopped** en `backend/docker-compose.yml` → evita caída del server por race condition con postgres en startup.
- **fix attachments volume** (Nacho, `ede3ca8`) → mount correcto del volumen de adjuntos en docker-compose.
- **docker-compose.yml raíz renombrado a `.bak`** → evita confusión con `backend/docker-compose.yml`. El archivo raíz era obsoleto.

### Impacto
El stack puede ahora levantarse en producción sin intervención manual. El docker-compose activo es `backend/docker-compose.yml`.

---

## Evento principal 2: Auth real con JWT

### Cambios
- **AuthContext reescrito** (`ed1dfb5`): `login()` llama a `authService.login()` real, guarda token en `localStorage`.
- Token restaurado en mount → sesiones persistentes (no se pierde al recargar).
- `setOnUnauthenticated` limpia `localStorage` y `user` al expirar el token.

### Impacto
Se eliminó el mock hardcodeado. La webapp ahora requiere credenciales reales para operar. Este era el bloqueador para compartir acceso con el equipo.

---

## Evento principal 3: Schema nullish en CaseEvents

### Cambios
- **CaseEventSchema** (`e6d4c9d`): todos los campos opcionales usan `.nullish()` en lugar de `.optional()` → acepta campos `null` o ausentes del backend.
- Fix necesario porque el backend Go puede devolver `null` en campos opcionales.

### Impacto
Errores de parsing en el frontend al ver eventos con campos vacíos eliminados.

---

## Evento principal 4: Activity + ActivityDetail — UI fixes

### Cambios
- **fix(Activity)** (`2d268c9`): restaura `usePendingEventsPaginated` + paginación pendientes (había sido roto por refactor anterior).
- **fix** (`68d08c4`): Activity tabla col siniestro restaurada; ActivityDetail — fila resumen de mail, scroll interno (flex+min-h-0), Clasificación unificada, modal `z-[9999]` (evita que quede detrás de otros elementos).

---

## Evento principal 5: Backup DB y design system

### Cambios
- **data/backups/libra_legal_20260309_2124.sql.gz** (80 KB comprimido) — primer backup documentado en el repo. Captura estado de producción al 09/03/2026 21:24.
- **docs/design-system.md** (345 líneas) — primer documento de design system del proyecto. Creado junto con commit de Ali.

---

## Cron runs del día

| Hora  | Job                       | Resultado |
|-------|---------------------------|-----------|
| 23:00 | nightly-extraction (L4)   | OK        |

---

## Pendientes sin resolver (carry-over completo desde 2026-03-08)

- [ ] Reescribir prompt de Donna (rol: Ingestion — ingestion-document-summary-ar + ingestion-formal-review-ar)
- [ ] Reescribir prompt de Mike (rol: Extraction — extraction-claim-summary-ar + extraction-policy-summary-ar)
- [ ] Actualizar data-processing-specialist.md (deprecar / refactorizar)
- [ ] Reescribir prompt de Review (Red Team Verifier)
- [ ] Actualizar regressions.md (guardrail: git log al inicio de sesión)
- [ ] Actualizar friction-log.md (límites Ali vs. Woz en infra/config)
- [ ] Capturar conocimiento legal en long-term-memory.md (9 PDFs contestaciones)
- [ ] Resolver canal #litigios (mensajes de Juan no llegan a Ali)
- [ ] Prompt de Jess (negativas compatibles con schema real Donna)
- [ ] Trigger Modo 1 en ORCHESTRATION.md (cuándo Ali corre auditoría post-pipeline)
- [ ] Endpoint agent-key para claims (pipeline no puede consumir SISE directamente)
- [ ] Lógica de linkeo automático siniestro→caso (claim_id en cases = NULL)
- [ ] Branch `sesion/2025-12-23` — PR aún no abierto

## Pendientes de Juan (sin novedad hoy)

- Skills de todos los agentes para revisar y alinear prompts
- Secciones del template borrador demanda_directa
- Confirmación si action_type aplica a mediaciones
- Test canal #litigios
- Decisión endpoint agent-key para claims

---

## Estado del sistema al cierre (23:00)

- Ali: operativo (solo crons, sin sesión humana)
- Rachel: estable — clasificación LLM + adjuntos + body_clean en producción (sin cambios hoy)
- Branch activa: `main`
- Branch `sesion/2025-12-23`: PR aún no abierto
- **Docker stack**: activo en producción. docker-compose activo: `backend/docker-compose.yml`. El raíz fue renombrado a `.bak`.
- **Auth**: JWT real en producción (mock eliminado). Sesiones persistentes via localStorage.
- **DB backup**: `data/backups/libra_legal_20260309_2124.sql.gz` — snapshot al 09/03/2026 21:24.
- **Design system**: `docs/design-system.md` creado (345 líneas).
- **Participantes del día**: Ali (Woz) — commits de infra/frontend. Nacho — fix attachments volume.
